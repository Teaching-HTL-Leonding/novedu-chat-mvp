// The product name, shown wherever the app identifies itself: the browser tab
// title (app/layout.tsx), the status bar's brand line (components/nav-menu.tsx)
// and the sign-in screen, which carries it because it renders without the bar.
export const BRAND = "HTBLA Leonding - Novedu";
