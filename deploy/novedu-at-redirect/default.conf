# 302, not 301: the redirect is temporary, so browsers must not cache it.
# $request_uri keeps path and query string.
server {
    listen 80 default_server;
    server_tokens off;
    access_log off;

    location / {
        return 302 https://app.novedu.at$request_uri;
    }
}
