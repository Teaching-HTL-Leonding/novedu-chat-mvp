{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/Teaching-HTL-Leonding/novedu-chat-mvp/refs/heads/main/activities/tutors/tutor-yaml.schema.json",
  "$comment": "Generated from lib/tutors/schemas.ts by `npm run generate:schemas` — do not edit by hand.",
  "title": "Tutor YAML",
  "description": "Schema for a tutor definition: an AI chat tutor with an assembled system prompt.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Short machine-readable tutor id, e.g. fractions-de."
    },
    "name": {
      "type": "string",
      "description": "Human-readable tutor title."
    },
    "title": {
      "description": "Optional greeting shown to students on the empty chat instead of the default welcome message.",
      "type": "string"
    },
    "description": {
      "type": "string",
      "description": "Short description of what this tutor does. Shown to students below the welcome greeting."
    },
    "exampleQuestions": {
      "description": "Optional example questions shown to students below the description on the empty chat. Clicking one puts the question text into the chat input. At most 5 are shown; with more, a random 5 are picked per page load.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/exampleQuestion"
      }
    },
    "anonymous": {
      "default": true,
      "description": "Chats are anonymous by default: no link between the signed-in student and their chat is stored. Set to false to record which student each chat belongs to.",
      "type": "boolean"
    },
    "llm": {
      "$ref": "#/$defs/llm"
    },
    "tools": {
      "default": [],
      "description": "Optional built-in tools the tutor's model may call (e.g. random_number). Off by default; mention enabled tools in tutor_instructions so the model uses them.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/tutorToolName"
      }
    },
    "prompt": {
      "$ref": "#/$defs/prompt"
    }
  },
  "required": [
    "id",
    "name",
    "description",
    "llm",
    "prompt"
  ],
  "additionalProperties": false,
  "$defs": {
    "exampleQuestion": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Short clickable label shown on the welcome screen."
        },
        "question": {
          "type": "string",
          "minLength": 1,
          "description": "Full question text. Shown as a tooltip and placed into the chat input on click."
        }
      },
      "required": [
        "title",
        "question"
      ],
      "additionalProperties": false,
      "description": "An example question shown on the welcome screen."
    },
    "llm": {
      "type": "object",
      "properties": {
        "model": {
          "type": "string",
          "description": "Model used for this tutor."
        },
        "provider": {
          "default": "SCCH",
          "description": "The LLM provider serving the model. For Azure Foundry, model is the deployment name.",
          "type": "string",
          "enum": [
            "SCCH",
            "Azure Foundry"
          ]
        },
        "imageInput": {
          "default": true,
          "description": "Image uploads are enabled by default. Set to false to hide the upload UI for text-only tutors or non-vision-capable models.",
          "type": "boolean"
        }
      },
      "required": [
        "model"
      ],
      "additionalProperties": false,
      "description": "The model and provider that back this tutor."
    },
    "tutorToolName": {
      "type": "string",
      "enum": [
        "random_number"
      ],
      "description": "Name of a built-in tutor tool."
    },
    "prompt": {
      "type": "object",
      "properties": {
        "fragment_files": {
          "default": [],
          "description": "Optional fragment libraries used by this tutor.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/fragmentFileRef"
          }
        },
        "text_files": {
          "default": [],
          "description": "Optional plain-text files (markdown / source) embedded verbatim via {{file \"alias\"}} markers.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/textFileRef"
          }
        },
        "tutor_instructions": {
          "type": "string",
          "description": "The tutor's system prompt. When any fragment_files or text_files are declared this is a Handlebars template: place fragments inline with {{fragment \"alias.id\" key=\"v\"}} markers and embed text files with {{file \"alias\"}} (optionally {{file \"alias\" from=10 to=40}} for a line range; escape a literal {{ as \\{{). For single-file tutors it is the whole prompt."
        }
      },
      "required": [
        "tutor_instructions"
      ],
      "additionalProperties": false,
      "description": "The tutor system prompt: a host template with inline fragment markers."
    },
    "fragmentFileRef": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[^.]+$",
          "description": "Local alias for this library, used before the dot in `{{fragment \"alias.id\"}}`. Must not contain a dot."
        },
        "url": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(https?://|(?![A-Za-z][A-Za-z0-9+.-]*:).+)$",
          "description": "HTTP(S) URL or relative path to the fragment library."
        }
      },
      "required": [
        "id",
        "url"
      ],
      "additionalProperties": false,
      "description": "A reference to a fragment library by alias + URL."
    },
    "textFileRef": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[^.]+$",
          "description": "Local alias for this text file, used in `{{file \"alias\"}}`. Must not contain a dot."
        },
        "url": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(https?://|(?![A-Za-z][A-Za-z0-9+.-]*:).+)$",
          "description": "HTTP(S) URL or relative path to the plain-text file."
        }
      },
      "required": [
        "id",
        "url"
      ],
      "additionalProperties": false,
      "description": "A reference to a plain-text file (markdown / source) by alias + URL."
    }
  }
}
